# PDFMathTranslate-next 使用说明

安装位置：`D:\paper\PDFMathTranslate-next`

## 1. 查看版本

```powershell
cd D:\paper
.\PDFMathTranslate-next\pdf2zh-portable.ps1 --version
```

当前本地配置已默认启用 DeepSeek，配置文件在：

```text
D:\paper\PDFMathTranslate-next\home\.config\pdf2zh\config.v3.toml
```

## 2. 启动 Web 界面

```powershell
cd D:\paper
.\PDFMathTranslate-next\start-gui.ps1
```

看到下面这行后就是启动成功；PowerShell 窗口保持开着，不要等它自动结束：

```text
WebUI ready: http://127.0.0.1:7860
```

启动后浏览器打开：

```text
http://127.0.0.1:7860
```

停止 WebUI：在运行窗口里按 `Ctrl+C`，或者直接关闭这个 PowerShell 窗口。

如果 7860 端口被占用，可以换端口：

```powershell
.\PDFMathTranslate-next\start-gui.ps1 --server-port 7861
```

## 2.1 使用便携 EXE

发行目录中的 `PDFMathTranslate.exe` 是便携启动器。双击它会启动目录内的 Python 运行时和 WebUI，等待服务就绪后自动打开浏览器；关闭启动器窗口会停止后台服务。日志位于：

```text
PDFMathTranslate-next\logs\webui-launcher.log
```

迁移到另一台 Windows 电脑时，请复制整个 `PDFMathTranslate-next` 文件夹，不能只复制 `PDFMathTranslate.exe`。目录中包含 Python、依赖、字体和 BabelDOC 资源。若电脑提示缺少运行库，请安装微软 Visual C++ Redistributable x64。

也可以指定端口：

```powershell
.\PDFMathTranslate.exe --server-port 7861
```

## 2.2 自定义模型端点

打开 WebUI 的“设置”页，在翻译服务中选择 `OpenAICompatible`，填写：

1. `Base URL`：兼容 OpenAI Chat Completions 的接口根地址，通常以 `/v1` 结尾，不要重复填写 `/chat/completions`。
2. `Model`：服务商提供的模型名称，必须与该端点实际支持的名称一致。
3. `API Key`：对应服务商密钥。

点击“保存设置”。配置保存在发行目录的 `home\\.config\\pdf2zh\\config.v3.toml`，不会依赖另一台电脑原有的用户目录。API Key 只保存在本地配置文件中，请不要把包含该文件的压缩包上传到公共位置。

如果需要从源码重新生成启动器，在项目目录运行：

```powershell
.\build-portable-exe.ps1
```

## 3. 命令行翻译 PDF

最简单命令：

```powershell
cd D:\paper
.\PDFMathTranslate-next\pdf2zh-portable.ps1 .\transformer.pdf --lang-in en --lang-out zh --output .\PDFMathTranslate-next\output
```

只翻译指定页：

```powershell
.\PDFMathTranslate-next\pdf2zh-portable.ps1 .\transformer.pdf --pages 1-3 --output .\PDFMathTranslate-next\output
```

只输出双语 PDF：

```powershell
.\PDFMathTranslate-next\pdf2zh-portable.ps1 .\transformer.pdf --no-mono --output .\PDFMathTranslate-next\output
```

只输出中文 PDF：

```powershell
.\PDFMathTranslate-next\pdf2zh-portable.ps1 .\transformer.pdf --no-dual --output .\PDFMathTranslate-next\output
```

## 4. 使用 API Key 服务

DeepSeek 示例：

```powershell
.\PDFMathTranslate-next\pdf2zh-portable.ps1 .\transformer.pdf --lang-in en --lang-out zh --output .\PDFMathTranslate-next\output
```

OpenAI 兼容接口示例：

```powershell
.\PDFMathTranslate-next\pdf2zh-portable.ps1 .\transformer.pdf --openaicompatible --openai-compatible-base-url "https://你的接口/v1" --openai-compatible-api-key "你的KEY" --openai-compatible-model "模型名" --lang-in en --lang-out zh --output .\PDFMathTranslate-next\output
```

## 5. 预热资源

```powershell
.\PDFMathTranslate-next\pdf2zh-portable.ps1 --warmup
```

当前机器已经成功安装主程序并验证 `2.9.0` 可启动。BabelDOC 资源源已固定为 `modelscope`，避免优先走 GitHub 时出现大量下载日志或超时。

## 7. 安装版（推荐：可安装到任意目录）

发行目录 `dist\` 下的 `PDFMathTranslate-Setup-<版本>.exe` 是标准 Windows 安装程序（Inno Setup）。

1. 双击运行，向导中的「选择安装位置」页可指定任意目录
   - 默认（免管理员权限）：`%LOCALAPPDATA%\Programs\PDFMathTranslate`
   - 想装到 `C:\Program Files\...`：在向导首页选择「为所有用户安装」，会请求一次管理员权限
2. 安装完成后可从开始菜单启动；桌面图标需在向导中勾选
3. 卸载：开始菜单 →「卸载 PDFMathTranslate」，或 Windows「设置 → 应用」

静默安装（自带指定目录参数，适合批量部署）：

```powershell
.\PDFMathTranslate-Setup-2.9.0.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /DIR="D:\Apps\PDFMathTranslate"
```

数据与配置位置：

- 安装在可写目录时：安装目录下的 `home\`（配置 `home\.config\pdf2zh\config.v3.toml`、日志 `logs\`、输出 `pdf2zh_files\`）
- 安装在只读目录（如 Program Files）且未以管理员身份运行时：应用自动把数据放到 `%LOCALAPPDATA%\PDFMathTranslate`

自动更新：应用启动后会读取 GitHub Releases 的 `latest.json`，发现新版本时提示下载并以 `/SILENT` 静默重装（整包替换，配置与输出不受影响）。可在「设置 → 应用 → 启动时自动检查更新」中关闭。

手动构建安装包：

```powershell
powershell -ExecutionPolicy Bypass -File build\build-release.ps1
# 产物：dist\PDFMathTranslate-Setup-<版本>.exe 与 dist\latest.json
```

依赖：.NET 8 SDK（或项目同级目录的 `.dotnet-sdk`）、Inno Setup 6（`winget install JRSoftware.InnoSetup`）。
